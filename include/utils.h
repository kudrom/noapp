#ifndef UTILS_H
    #define UTILS_H
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>

    char *generate_length_filename(char *base_filename);

    int open_file(FILE *file);
#endif
